[project]
name = "axiom-tui"
version = "1.0.0"
description = "a small terminal text editor built with textual"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = ["textual>=1.0.0"]

[project.scripts]
axiom-tui = "main:run"
axiom = "main:run"
